<div class="new-site watch-webinar-page">
  <%= render "layouts/navbar/pages_dark", hide_login: true %>

  <div class="container">
    <div class="row">
      <div class="col-sm-10 col-sm-offset-1">
        <div class="webinar">
          <% if @participant %>
            <div class="webinar-video">
              <div class="video-wrapper">
                <%= embedded_video("https://www.youtube.com/embed/#{@webinar.event_url}") %>
              </div>
            </div>
            <div class="webinar-info"><%= @webinar.description %></div>
          <% else %>
            <div class="webinar-info">¡Lo sentimos! El link no es correcto, revisa que lo hayas copiado bien e intenta nuevamente.</div>
          <% end %>
        </div>
      </div>
    </div>
  </div>

</div>
