<%# require: resource %>
<div class="col-sm-10 col-sm-offset-1 expand text-center external">
  <h1><%= resource.title %></h1>
  <p><%= resource.description %></p>
  <a href="<%= resource.url %>" target="_blank" class="btn btn-default btn-lg">
    Haz click acá para abrirlo &nbsp;
    <span class="glyphicon glyphicon-new-window"></span>
  </a>
</div>
