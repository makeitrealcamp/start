<div class="container resource-form-page">
  <div class="row">
    <%= form_for [@course, @resource]  do |f| %>
      <div class="col-sm-10 col-sm-offset-1">
        <h1>Nuevo Recurso</h1>
        <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :description %>
          <%= f.text_area :description, rows: 2, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :type %>
          <%= f.select :type, types_resources, { prompt: true } , {class: 'form-control'} %>
        </div>

        <div class="form-group">
          <%= f.label :url %>
          <%= f.text_field :url, class: "form-control" %>
        </div>

        <div class="actions text-right">
          <%= link_to "Cancelar", @course, class: "btn btn-default" %>
          <%= f.submit class: "btn btn-primary"%>
        </div>
      </div>
    <% end %>
  </div>
</div>
