<div class="container course-form-page">
  <div class="row">
    <%= form_for :activate_user, url: activate_users_path, method: :patch do |f| %>
      <div class="col-sm-10 col-sm-offset-1">
        <h1>Activar Cuenta</h1>
        <div class="row">
          <div class="col-sm-6">
            <%= render 'shared/flash_messages_form', object: @activate_user %>
            <div class="form-group">
              <%= f.label :email, "Correo Electrónico" %>
              <%= f.text_field :email, class: 'form-control input-lg', placeholder: 'email', disabled: true %>
            </div>

            <div class="form-group">
              <%= f.label :birthday, "Fecha de cumpleaños" %>
              <%= f.text_field :birthday, data: { datetimepicker: true }, class: "form-control input-lg", placeholder: "DD/MM/AAAA", value: @activate_user.birthday.blank? ? '' : Date.parse(@activate_user.birthday).strftime('%d-%m-%Y') %>
            </div>
            <div class="form-group">
              <%= f.label :mobile_number, "Celular"%>
              <%= f.text_field :mobile_number, data: { intlTelInput: true }, class: 'form-control input-lg', placeholder: '' %>
              <span>(p.e. + 57 3002111111)</span>
            </div>
          </div>
          <div class="col-sm-6">
            <div class="form-group">
              <%= f.label :nickname %>
              <div class="input-group input-group-lg">
                <span class="input-group-addon" >makeitreal.camp/u/</span>
                <%= f.text_field :nickname, class: 'form-control'%>
              </div>

              <span class="help-block">Nickame se usa para determinar la  URL personalizada, si  por ejemplo la url es www.makeitreal.camp/u/maker,
              ¡simplemente tienes que escribir "maker"!. </span>
            </div>

            <div class="form-group">

              <span class="help-block">En <b>make it real</b> usamos gravatar <a href="https://es.gravatar.com/">https://es.gravatar.com/</a>
                para la imagen del perfil, para cambiar tu imagen de perfil debes hacerlo abriendo una cuenta en Gravatar y subirlo por ahi.</span>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-md-6 col-md-offset-6">
            <div class="actions text-right">
              <%= f.submit("Activar Cuenta", class: "btn btn-primary") %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
