<% if @user.valid? %>
  $(".content-wrapper").prepend("<%= j render 'shared/alert', msg: 'Perfil actualizado', type: 'notice' %>")

  $(".profile-header").html("<%= j render 'users/profile_header', user: @user, is_own_profile: @is_own_profile %>");

<% else %>
  $(".content-wrapper").prepend("<%= j render 'shared/alert', msg: 'Los datos del perfil son inválidos', type: 'error' %>")
<% end %>
