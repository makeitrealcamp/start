<div class="container quiz-attempt-result">
  <div class="row">
    <div class="col-xs-12">
      <%= link_to course_quizer_quiz_path(@course,@quiz) do %>
        <span class='glyphicon glyphicon-arrow-left'></span>
        Volver al Quiz <%= @quiz.name %>
      <% end %>

      <h1>Resultados para el quiz <strong><%= @quiz_attempt.quiz.name %></strong></h1>
      <p>
        Puntaje: <%= "%.2f\%" % (@quiz_attempt.score*100) %>
      </p>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-12">
      <h1><%= @quiz.name %></h1>
    </div>
  </div>

  <% @quiz_attempt.question_attempts.each do |qa| %>
    <div class="row question">
      <div class="col-xs-12">
        <%= render "quizer/questions/multi_answer_question_result", question_attempt: qa %>
      </div>
    </div>
  <% end %>
</div>
