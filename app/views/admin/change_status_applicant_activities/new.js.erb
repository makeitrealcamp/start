$('<%= j render "modal" %>').on('shown.bs.modal', function() {
  new ChangeRejectReasonView();
  new ChangeSecondInterviewSubstateView();
}).on('hidden.bs.modal', function (e) {
  $(this).remove();
  $('.modal-backdrop').remove();
}).modal();
