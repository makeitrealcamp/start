$('<%= j render "modal" %>').on('shown.bs.modal', function() {
    var templates = <%= EmailTemplate.all.to_json.html_safe %>;
    new SendEmailView({ templates: templates });
  }).on('hidden.bs.modal', function (e) {
    $(this).remove();
    $('.modal-backdrop').remove();
  }).modal();
