<%# required: notification, notification.data["point_id"] %>
<% point = Point.find(notification.data["point_id"]) %>

<%= notification.user.first_name %> ha ganado <strong><%= point.points %> puntos</strong>
<% if point.pointable_type == "Challenge" %>
  por resolver el reto <strong><%= point.pointable.subject.name %> - <%= point.pointable.name %></strong>
<% elsif point.pointable_type == "Project" %>
  por su solución para el proyecto <strong><%= point.pointable.subject.name %> <%= point.pointable.name %></strong>
<% end %>
